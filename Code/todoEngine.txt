-------------------------------------------------------------------------------
Core
-------------------------------------------------------------------------------
- макрос SE_ASSERT - на данный момент плохо работает. переделать
- Subsystem - мне не нравится что в данном классе нужно вручную вызывать Destroy. нужно найти решение
- Window::DPI - сделать.  посмотреть как сделанно в SDL/GLFW/SFML/unity/ue
- Window - в нем есть следующие избыточные члены: m_windowX, m_windowY, m_windowWidth, m_windowHeight - используются только при переходе в фулскрин/обратно. нужно переделать
- OSPlatform - сделать в них методы для загрузки DLL и функций из них (на данный момент используется в bool Window::initDPI() LoadLibraryW и GetProcAddress / в D3D11RenderDevice)
- доработать Error
- template <typename T> T pi = (T)3.141592 - шаблонные переменные




-------------------------------------------------------------------------------
Math
-------------------------------------------------------------------------------
- ExtentXD перенести в Size
- убрать QuaternionF и использовать Quaternion(переделать из шаблона в класс) (хм, а double?)

-------------------------------------------------------------------------------
Render
-------------------------------------------------------------------------------
- временно использую ComPtr - лучше переписать на свой аналог
- но между разными  версиями OpenGL/GLES/WebGL есть общий код, стоит ввести общий для них (как с DX)
- сейчас в DX11 используется дефолтный адаптер (указывается в D3D11CreateDevice)
- сделать feature level (1/2/3)

-------------------------------------------------------------------------------
User Code
-------------------------------------------------------------------------------
- основной класс
	сейчас пользователь создает класс содержащий Init/Frame/Update/Close. 
	Проблема в том что в реальности весь игровой код будет в Frame/Update.
	Но Init и Close всеже нужны (например чтобы прицепить события для обработки клава-мыши)
	Решение: сделать систему на подобие той что я называл системой модулей - то есть некий список состояний, при этом активно только одно состояние, и оно может передать свою активность другому (который вызовет инициализацию)
	

-------------------------------------------------------------------------------
Разное
-------------------------------------------------------------------------------
- использовать https://github.com/microsoft/STL
